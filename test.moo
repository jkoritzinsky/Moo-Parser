int x;
int y;

bool a;
bool b;
void c;

struct node myNode;

struct a { int b; };

int empty() {
}

int noparams()
{
return;
}

int params(int a, bool b) {
return a;
}

void expressions_galore() {
  int q;
  q = e.f.g;
  q = -a < b && b > a || b >= b && a == b && c <= b && a != c;
  return params((a+b)*c-d/3, "test", true && b || false);
}

void statements_galore() {
  if(a)
  {
    return b;
  }
  a++;
  b--;
  if(c == d)
  {
  void ge;
   a = b;
   }
   else
   {
   return z;
   }
   
   while(true) {
   int w;
   return 3;
   }
   
   expressions_galore();
   
   cin >> a;
   cout << b;
}
